'use strict';

/**
 *  unreal-attributess controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::unreal-attributess.unreal-attributess');
