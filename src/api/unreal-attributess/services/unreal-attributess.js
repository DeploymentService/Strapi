'use strict';

/**
 * unreal-attributess service.
 */

const { createCoreService } = require('@strapi/strapi').factories;

module.exports = createCoreService('api::unreal-attributess.unreal-attributess');
